{
  "name": "node",
  "version": "1.0.0",
  "description": "Questa cartella contiene esempi pratici su come gestire richieste HTTP in **Node.js** utilizzando il framework **Express**. Include esempi di richieste **GET**, **POST** e una semplice API REST.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2",
    "express-validator": "^7.2.1"
  }
}
